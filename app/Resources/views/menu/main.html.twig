
<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
            </button>
            <a class="navbar-brand page-scroll" href="#page-top">eZ Starter</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">

                <li>
                    <a href="{{ path('ez_urlalias', {'locationId': 2}) }}">Front Page</a>
                </li>

                {% for menu_item in menu_items %}
                <li>
                    <a href="{{ path(menu_item.valueObject) }}">{{ ez_content_name(menu_item.valueObject.contentInfo) }}</a>
                </li>
                {% endfor %}

                {% if app.user %}
                    <li>
                        <a href="{{ path('logout')}}">Logout</a>
                    </li>
                    <li>
                        <a href="#profil"><span class="glyphicon glyphicon-user" aria-hidden="true"></span>&nbsp;<small>Hello {{ app.user.username }}!</small></a>

                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_ez_user_register')}}">Register</a>
                    </li>
                    <li>
                        <a href="{{ path('login')}}">Login</a>
                    </li>
                {% endif %}



            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
